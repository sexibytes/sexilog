client:
  hosts:
    - 127.0.0.1
  port: 9200
  url_prefix:
  use_ssl: False
  certificate:
  client_cert:
  client_key:
  ssl_no_validate: False
  username: "elastic"
  password: "Sex!L0g"
  timeout: 30
  master_only: False

logging:
  loglevel: INFO
  logfile: '/var/log/elasticsearch-curator/curator.log'
  logformat: default
  blacklist: ['elasticsearch', 'urllib3']